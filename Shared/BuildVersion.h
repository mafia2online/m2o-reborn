#ifndef __build_version
#define __build_version

#define NETWORK_PLATFORM_ID          121 // random number setting current game platform id, to prevent connections from different games/mods, in future
#define NETWORK_PROTOCOL_VERSION     1   // version of protocol, should be changed if major, incompatible changes are introduced
#define NETWORK_BUILD_VERSION        1   // version of build. server owner can decide, should clients should be refused or allowed joining the game

#endif // __build_version
